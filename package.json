{
  "name": "quick-image-board",
  "version": "0.1.0",
  "private": true,
  "description": "本地优先的桌面图片临时画布与分类工具",
  "main": "dist-electron/electron/main/index.js",
  "scripts": {
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"npm run build:electron && wait-on http://127.0.0.1:5173 && cross-env NODE_ENV=development node_modules/electron/dist/electron.exe .\"",
    "build:renderer": "vite build",
    "build:electron": "tsc -p tsconfig.node.json",
    "build": "npm run build:renderer && npm run build:electron",
    "typecheck": "tsc -p tsconfig.node.json --noEmit && tsc --noEmit",
    "test": "npm run build:electron && node --test tests/storage.test.mjs tests/ui-surface.test.mjs tests/windows-clipboard.test.mjs tests/window-geometry.test.mjs tests/window-main.test.mjs tests/shared-boundary.test.mjs tests/shared-geometry.test.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^32.2.7",
    "typescript": "^5.7.2",
    "vite": "^5.4.11",
    "wait-on": "^8.0.1"
  }
}
